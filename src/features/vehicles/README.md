# `features/vehicles`

Internal vehicle domain model, Zod validation, query helpers, and server-side mock inventory.

- **Client-safe imports:** `@/features/vehicles` (types, constants, utils, schemas)
- **Server-only:** `@/features/vehicles/server`, `@/services/vehicle.service`

See [`docs/vehicle-domain.md`](../../../docs/vehicle-domain.md).
