# `features/vehicle-listings`

Vehicle inventory browsing: search, filtering, listing detail pages, and related UI.

Will eventually consume stock data sourced via `integrations/autotrader` and `services`.
No implementation yet — foundation phase only.
