# `server/db`

Database access layer. Will house the Prisma client singleton and query modules once
Prisma + PostgreSQL are introduced.

Intentionally empty. This folder is reserved for a future phase — do not implement yet.
