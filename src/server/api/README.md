# `server/api`

Shared helpers for Next.js Route Handlers under `src/app/**/route.ts`
(response formatting, error mapping, request validation).

Intentionally empty. This folder is reserved for a future phase — do not implement yet.
